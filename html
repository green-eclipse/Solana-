<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Reward the Trench Soldier</title>

<style>
:root {
  --bg: #080b14;
  --card: #11162a;
  --text: #e9ecf5;
  --muted: #9aa3c7;
  --accent: #7c8cff;
  --accent2: #00ffd5;
}

* {
  box-sizing: border-box;
  font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: radial-gradient(1200px 500px at 50% -10%, #151b3d, var(--bg));
  color: var(--text);
  overflow-x: hidden;
}

/* FLOATING GLOW ORBS */
.orb {
  position: fixed;
  width: 400px;
  height: 400px;
  filter: blur(120px);
  opacity: 0.25;
  z-index: -1;
  animation: float 18s infinite alternate ease-in-out;
}

.orb.one {
  background: var(--accent);
  top: -120px;
  left: -120px;
}

.orb.two {
  background: var(--accent2);
  bottom: -150px;
  right: -150px;
  animation-delay: 6s;
}

@keyframes float {
  0% { transform: translateY(0px); }
  100% { transform: translateY(80px); }
}

/* HERO */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 80px 20px;
}

.hero-inner {
  max-width: 820px;
  animation: fadeUp 1.1s ease forwards;
}

.badge {
  display: inline-block;
  padding: 8px 18px;
  border-radius: 999px;
  background: rgba(124,140,255,0.15);
  color: var(--accent);
  font-weight: 600;
  margin-bottom: 22px;
}

h1 {
  font-size: clamp(2.5rem, 5vw, 4rem);
  line-height: 1.1;
  margin-bottom: 18px;
}

.hero p {
  color: var(--muted);
  font-size: 1.15rem;
  margin-bottom: 36px;
}

/* CTA */
.cta {
  display: inline-block;
  padding: 18px 34px;
  border-radius: 16px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  color: #05070d;
  font-weight: 800;
  box-shadow: 0 0 0 rgba(0,255,213,0.6);
  animation: pulse 2.2s infinite;
}

@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(0,255,213,0.45); }
  70% { box-shadow: 0 0 0 22px rgba(0,255,213,0); }
  100% { box-shadow: 0 0 0 0 rgba(0,255,213,0); }
}

/* SECTIONS */
.container {
  max-width: 1100px;
  margin: auto;
  padding: 0 20px;
}

.section {
  margin: 120px 0;
  opacity: 0;
  transform: translateY(60px);
  transition: all 0.8s ease;
}

.section.visible {
  opacity: 1;
  transform: translateY(0);
}

.card {
  background: linear-gradient(180deg, rgba(255,255,255,0.05), rgba(255,255,255,0.01));
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 26px;
  padding: 36px;
  box-shadow: 0 30px 80px rgba(0,0,0,0.55);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.card:hover {
  transform: translateY(-6px);
  box-shadow: 0 40px 110px rgba(0,0,0,0.7);
}

.section h2 {
  font-size: 2.2rem;
  margin-bottom: 14px;
}

.section p {
  color: var(--muted);
}

/* MEME IMAGE */
.meme {
  margin-top: 28px;
  border-radius: 22px;
  overflow: hidden;
  border: 1px solid rgba(255,255,255,0.1);
}

.meme img {
  width: 100%;
  display: block;
}

/* GRID */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
  gap: 22px;
  margin-top: 30px;
}

.pill {
  padding: 22px;
  border-radius: 18px;
  background: rgba(124,140,255,0.08);
  border: 1px solid rgba(124,140,255,0.2);
}

.pill strong {
  display: block;
  margin-bottom: 8px;
  color: var(--accent);
}

/* WALLET */
.wallet {
  margin-top: 20px;
  padding: 20px;
  border-radius: 16px;
  background: #05070d;
  border: 1px dashed rgba(255,255,255,0.25);
  font-family: monospace;
  word-break: break-all;
}

footer {
  text-align: center;
  padding: 80px 20px;
  color: #6f7795;
}

/* ANIMATIONS */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to { opacity: 1; transform: translateY(0); }
}
</style>
</head>

<body>

<div class="orb one"></div>
<div class="orb two"></div>

<!-- HERO -->
<section class="hero">
  <div class="hero-inner">
    <div class="badge">Solana • Meme Contest • Trench Certified</div>
    <h1>I Saw the Vision<br>and I Ran With It 🚁🎁</h1>
    <p>
      Solana doesn’t reward noise.<br>
      It rewards culture, timing, and execution.
    </p>
    <a href="#reward" class="cta">Send the loot</a>
  </div>
</section>

<div class="container">

  <!-- MEME -->
  <section class="section">
    <div class="card">
      <h2>The Meme</h2>
      <p>
        This wasn’t random. It was aligned with the narrative and delivered clean.
      </p>
      <div class="meme">
        <img src="meme.jpg" alt="Meme Submission">
      </div>
    </div>
  </section>

  <!-- WHY -->
  <section class="section">
    <div class="card">
      <h2>Why It Deserves a Reward</h2>
      <div class="grid">
        <div class="pill">
          <strong>🧠 Cultural Fit</strong>
          Native Solana humor that feels natural, not forced.
        </div>
        <div class="pill">
          <strong>📈 Narrative Push</strong>
          Strengthened the identity without overexplaining.
        </div>
        <div class="pill">
          <strong>🫡 Trench Respect</strong>
          Memeable, shareable, and self-aware.
        </div>
        <div class="pill">
          <strong>⚡ Clean Execution</strong>
          Simple, professional, and effective.
        </div>
      </div>
    </div>
  </section>

  <!-- REWARD -->
  <section id="reward" class="section">
    <div class="card">
      <h2>Reward Address</h2>
      <p>
        If this contributed to the momentum, you already know what to do.
      </p>
      <div class="wallet">
        CPHwzr7p7UcpoASiNCJghq7ZK4VtYHiWbwYGm8EVLw2q
      </div>
      <p style="margin-top:10px;">Network: <strong>$SOL (Solana)</strong></p>
    </div>
  </section>

</div>

<footer>
  Built for culture. Delivered with intent.
</footer>

<script>
const observer = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add("visible");
    }
  });
}, { threshold: 0.15 });

document.querySelectorAll(".section").forEach(section => {
  observer.observe(section);
});
</script>

</body>
  </html>
